.venv/
__pycache__/
*.pyc
.env
data/devset/
data/devset_ovlp2/
data/devset_ovlp_aug/
data/wavlm_frame_cache/
data/external/
data/*.json
.DS_Store
uploads/
_panns_cache/

# Confidential production call data (trial PDF: "do not upload to unapproved
# public services"). Kept locally for reproducing eval/score_labels.py etc.,
# never committed. See README.md's "Provided call recordings" note.
requirements/*.ogg
requirements/labels.csv
requirements/*.pdf
drive_requirements/
data/eval_batch.zip

# Never commit exported Claude Code session transcripts — they can contain
# real secrets typed or diffed during a session (this repo learned that the
# hard way; see git history cleanup).
*.txt
!requirements.txt
!requirements-dev.txt
